{
  "name": "dragon-bugs-site",
  "version": "1.0.0",
  "description": "Static site for dragon bugs logs",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "node --watch app.js",
    "build": "node build.js",
    "build:github": "node build.js && node build_github.js",
    "deploy": "gh-pages -d dist-github"
  },
  "dependencies": {
    "ejs": "^3.1.9",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=20.18.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
